name: Verify Agent Notes

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Verify note tree, format, and archive seals
        env:
          # HEAD is a no-op on a clean checkout. Compare against the pre-change
          # commit: PR base, or the previous tip of the pushed branch.
          AGENT_NOTE_ARCHIVE_BASE_REF: ${{ github.event.pull_request.base.sha || github.event.before }}
        run: |
          npx tsx scripts/agent-notes/verify-agent-note-tree.ts
          npx tsx scripts/agent-notes/verify-agent-note-format.ts
          npx tsx scripts/agent-notes/verify-archived-agent-notes.ts
