# 模型评估（internal/eval）

评估模块回答一个问题：这个免费渠道的模型现在到底还能不能用、质量如何。管理台提供当前评估、队列、历史、排序四个视图。

## 怎么做的

- **队列状态机**：`ModelEvalQueueTask` 以 queued / running 推进；running 有 15 分钟租约，过期由调度器回收。
- **崩溃恢复**：单实例 SQLite 启动立即把遗留 running 退回 queued；worker panic recover 后 MarkDone 失败置 abandoned，留给 Stop 阶段 requeue；停机只退回本进程未完成任务。
- **单实例互斥**：评估队列入队用 MySQL `GET_LOCK` / PostgreSQL advisory lock 互斥，防止多副本重复派发。
- **双通道刷新**：SSE 推送 + 5 秒轮询互备，写缓存前取消对应在途查询。
- **确定性失败清理**：401 / 403 / 404 这类确定性失败累计 3 次自动删除渠道模型，不再浪费队列额度。
- **排序语义**：`ok` 与 `violation` 都可进排序，`priority` 从 1 递增且数值越小越优先，单个任务执行超时 10 分钟；面板按密钥测试可选模型、评估侧按分档注入 `max_tokens` 防大额计费。

## 设计想法

- **评估在网关内做而不是独立脚本**：评估直接复用渠道配置、Key 轮询与探测基建，结果天然写回库内供分组路由参考；独立脚本要重建一整套凭据与出站上下文，得不偿失。
- **租约 + abandoned 双保险**：评估任务最长 10 分钟，进程随时可能被杀——租约保证过期任务被回收，abandoned 标记保证 MarkDone 失败的任务在停机路径仍有 requeue 机会，评估队列不会因崩溃永久卡死。
- **确定性失败快速删除**：免费渠道大量 401/403/404 是"凭据已死"而非抖动，与其反复评估，不如三次定论自动清出列表。

## 已知边界

- 管理台「加入排序」按钮仍只对 `ok` 可点，比 `from-history` 接口更窄——在未单独决定前不放宽。

## 深入阅读

- 队列互斥与日志实时观测决策：`.agents/notes/implemented/` 下 eval 相关笔记（全仓搜索 `model-eval` / `eval-queue`）
