# 06 - maskit 关键源码参考

> 本文档为 maskit Python 原文/移植要点的「参考材料」，记录「当初移植自哪里」，不是「当前 NovaVeil 如何运转」的事实文档；因果决策见 `.agents/notes/implemented/feature/2026-09-10-gateway-native-request-masking.md` 等 agent notes。当前事实以上文其余章节（README/01~04/07）为准。

> **状态：已实现**（2026-09-10，REQ-015）。maskit 源码片段已用 Go 重写并落地于 `internal/relay/mask/`，本文档保留 Python 原文作移植参考。
> 本文档保留 maskit `engine/transparent.py` 中最值得 NovaVeil 移植的关键源码片段，
> 按模块组织，标注源码行号与移植要点。Python 源码原样保留，移植时用 Go 重写。
> 设计思路见 `05-借鉴思路总结.md`。

---

## 一、占位符生成

> 源码：`transparent.py:636-681`

```python
_TOKEN_ALPHABET = "bcdfghjklmnpqrstvwxz"  # 20 个辅音，不含元音与数字

_LABEL_SAFE_RX = re.compile(r"[^A-Z0-9]+")

def _safe_label(label):
    """标签 ASCII 化。内置规则标签本身是 ASCII；自定义中文标签统一归 TERM。"""
    up = _LABEL_SAFE_RX.sub("", str(label or "").upper())
    return up[:12] or "TERM"

def _rand_suffix():
    """占位符后缀：6 位纯辅音。
    用 secrets.choice 而不是 random：可预测的后缀会让上游能枚举、关联同一实体。"""
    return "".join(secrets.choice(_TOKEN_ALPHABET) for _ in range(6))

def _new_token(label):
    """生成不与近期占位符冲突的新占位符。"""
    lab = _safe_label(label)
    for _ in range(20):
        token = "{{%s_%s}}" % (lab, _rand_suffix())
        if token not in _RECENT_REV:
            return token
    # 兜底仍用 6 位：长度必须落在还原正则认得的范围内。
    # 曾返回 secrets.token_hex(6)（12 字符），正则只认 6 → 永远匹配不到 → 还原永久失败
    return "{{%s_%s}}" % (lab, _rand_suffix())

def _placeholder(label, fwd):
    """Use random per-session placeholders; never derive IDs from secrets."""
    existing = set(fwd.values())
    for _ in range(20):
        token = _new_token(label)
        if token not in existing:
            return token
    return _new_token(label)
```

**Go 移植要点**：`secrets.choice` → `crypto/rand`；`_RECENT_REV` 查重 → map 查存在。

---

## 二、双向映射表与多轮复用

> 源码：`transparent.py:684-838` + `1809-1828`

```python
# 跨请求占位符复用表（仅内存，不落盘）
_RECENT_FWD = {}   # orig -> [token, label, ts]
_RECENT_REV = {}   # token -> [orig, label, ts]
_RECENT_MAX = 2000
RECENT_TTL = 24 * 3600  # 24h，独立于会话 TTL

def _recall_token(orig, label):
    """取该原文的占位符：TTL 内复用旧的，否则新建并登记。"""
    # 安全防套娃：如果 orig 自身就是占位符，严禁为其分配新 token！
    if isinstance(orig, str) and _PLACEHOLDER_RX.match(orig):
        rec = _RECENT_REV.get(orig)
        if rec and not _PLACEHOLDER_RX.match(rec[0]):
            orig = rec[0]
            label = rec[1] or label
        else:
            return orig  # 查不到真实明文，原样返回，绝不套娃

    now = time.time()
    hit = _RECENT_FWD.get(orig)
    if hit and now - hit[2] <= _recent_ttl():
        hit[2] = now          # 滑动续期
        rev = _RECENT_REV.get(hit[0])
        if rev:
            rev[2] = now
        return hit[0]
    token = _new_token(label)
    _RECENT_FWD[orig] = [token, label, now]
    _RECENT_REV[token] = [orig, label, now]
    _prune_recent(now)
    return token

def _prune_recent(now=None):
    """按 TTL + 条数上限清理复用表，防止无界增长。"""
    now = now or time.time()
    ttl = _recent_ttl()
    stale = [k for k, v in list(_RECENT_FWD.items()) if now - v[2] > ttl]
    for k in stale:
        tok = _RECENT_FWD.pop(k, [None])[0]
        _RECENT_REV.pop(tok, None)
    if len(_RECENT_FWD) > _RECENT_MAX:
        oldest = sorted(list(_RECENT_FWD.items()), key=lambda kv: kv[1][2])
        for k, v in oldest[: len(_RECENT_FWD) - _RECENT_MAX]:
            _RECENT_FWD.pop(k, None)
            _RECENT_REV.pop(v[0], None)

def _touch_recent(token, orig, now=None):
    """命中即续期（滑动过期）。还原侧也要刷，否则只出现一次后不再脱敏的映射会过期。"""
    now = now or time.time()
    rev = _RECENT_REV.get(token)
    if rev is not None:
        rev[2] = now
    fwd = _RECENT_FWD.get(orig)
    if fwd is not None and fwd[0] == token:
        fwd[2] = now
```

**Go 移植要点**：两个 `map` + `sync.RWMutex`；TTL 用 `time.Time`；NovaVeil 可挂到 `X-Session-Id` 会话维度。

---

## 三、脱敏主流程

> 源码：`transparent.py:1662-1691`（跳过占位符）+ `1694-1804`（mask 主函数核心段）

```python
def _mask_excluding_placeholders(text, rx, sub_fn):
    """对 text 做正则替换，但跳过已有的占位符片段（防污染）。
    自定义词里 2 字符的 hex 子串会把占位符劈开 → 畸形占位符 → 还原永久失败。"""
    if not text:
        return text
    result = []
    last_end = 0
    found_placeholder = False
    for m in _PLACEHOLDER_RX.finditer(text):
        found_placeholder = True
        before = text[last_end:m.start()]
        result.append(rx.sub(sub_fn, before))   # 占位符之前的片段：做替换
        result.append(m.group())                 # 占位符本身：原样保留
        last_end = m.end()
    tail = text[last_end:]
    if tail:
        result.append(rx.sub(sub_fn, tail))
    if not found_placeholder:
        return rx.sub(sub_fn, text)
    return "".join(result)


def mask(text, sid):
    """脱敏文本。返回脱敏后的文本。"""
    if not text:
        return text
    s = sessions.get(sid)
    fwd = s["fwd"]; labels = s["labels"]; rev = s["rev"]
    hit_orig = set(); new_orig = set()

    def _hit(orig, label="API_KEY"):
        if orig not in fwd:
            new_orig.add(orig)
            _remember(fwd, labels, orig, label)
            rev[fwd[orig]] = orig   # 增量维护，避免每次全量重建
        hit_orig.add(orig)

    # 1. Key 前缀规则
    if _rule_enabled("API_KEY"):
        prefix_rx = _prefix_secret_regex()
        if prefix_rx:
            def _prefix_sub(m):
                _hit(m.group())
                return fwd.get(m.group(), m.group())
            text = _mask_excluding_placeholders(text, prefix_rx, _prefix_sub)

    # 2. 自定义敏感词（长词优先，单次扫描）
    cw_rx = _custom_combined_regex()
    if cw_rx:
        def _cw_sub(m):
            word = m.group(0)
            _hit(word, _cw_label_lower.get(word.lower(), ""))
            return fwd.get(word, word)
        text = _mask_excluding_placeholders(text, cw_rx, _cw_sub)

    # 3. 内置正则规则
    for rx, label, group_idx in RULES:
        if not _rule_enabled(label) or not _rule_may_hit(text, label):
            continue
        matched = []
        for m in rx.finditer(text):
            orig = m.group(group_idx)
            # 校验函数防误报
            if label == "CARD" and not _card_ok(orig): continue
            if label == "IDCARD" and not _idcard_ok(orig): continue
            if label == "PHONE" and not _phone_ok(orig): continue
            # ... 其余校验
            _hit(orig, label)
            matched.append(orig)
        # 去重替换防 O(n²) 退化（同一手机号出现上万次 → 只替换唯一原文）
        if matched:
            unique_orig = list(dict.fromkeys(matched))  # 去重保序
            repl_map = {orig: fwd[orig] for orig in unique_orig}
            def _rule_sub(m):
                orig = m.group(group_idx)
                if orig in repl_map:
                    if group_idx == 0:
                        return repl_map[orig]
                    # group_idx > 0：只替换捕获组，保留其余文本（如 Bearer sk-xxx 只换 sk-xxx）
                    gs, ge = m.span(group_idx)
                    return m.group(0)[:gs - m.start()] + repl_map[orig] + m.group(0)[ge - m.start():]
                return m.group(0)
            text = _mask_excluding_placeholders(text, rx, _rule_sub)

    s.setdefault("last_hits", set()).update(hit_orig)
    s.setdefault("new_orig", set()).update(new_orig)
    return text
```

**Go 移植要点**：`_PLACEHOLDER_RX.finditer` → `regexp.FindAllStringIndex`；`dict.fromkeys` 去重 → `map` key 去重；校验函数逐个移植。

---

## 四、还原主流程

> 源码：`transparent.py:1830-1951`

```python
def _lookup(token, sid):
    """占位符 → 原文。先查本会话，再查跨请求复用表，最后做套娃解包。
    绝不做模糊匹配、不猜、不推算。"""
    s = sessions.get(sid)
    hit = None
    if s:
        hit = s["rev"].get(token)
        if hit is not None:
            _touch_recent(token, hit)
    if hit is None:
        recent = _RECENT_REV.get(token)
        if recent and time.time() - recent[2] <= _recent_ttl():
            _touch_recent(token, recent[0])
            hit = recent[0]
    # 防占位符套娃解包（递归直到真实明文，深度上限 5）
    depth = 0
    while hit is not None and isinstance(hit, str) and _PLACEHOLDER_RX.match(hit) and depth < 5:
        depth += 1
        inner = s["rev"].get(hit) if s else None
        if inner is None:
            rec = _RECENT_REV.get(hit)
            if rec: inner = rec[0]
        if inner is not None and inner != hit:
            hit = inner
        else:
            break
    if hit is not None and isinstance(hit, str) and _PLACEHOLDER_RX.match(hit):
        return None  # 最终还是占位符自身，说明没有真实明文
    return hit


def restore(text, sid, channel="", escape=False, final=False):
    """把占位符还原成原文。
    channel: 流式通道标识，半截占位符只在本通道缓冲。
    escape:  目标是 JSON 字符串内部（tool 参数），原文需 JSON 转义。
    final:   True = 不再等后续 chunk，缓冲区一次性吐出。"""
    s = sessions.get(sid)
    if not s or not isinstance(text, str):
        return text
    pend = s["pending"]
    buf = pend.get(channel, "") + text
    if final:
        pend.pop(channel, None)
        confirmed = buf
    else:
        m = _PARTIAL_RX.search(buf)  # 尾部是否是未闭合的占位符前缀
        if m and m.end() == len(buf) and len(m.group()) <= _PARTIAL_MAX:
            confirmed = buf[:m.start()]   # 前面是确认内容
            pend[channel] = m.group()     # 未闭合部分暂存
        else:
            confirmed = buf
            pend.pop(channel, None)
    if not confirmed:
        return ""

    def _sub(m):
        token = m.group(0)
        orig = _lookup(token, sid)
        if orig is None:
            s["unresolved"] = s.get("unresolved", 0) + 1  # 查不到就原样返回，但必须计数
            return token
        s["restored"] = s.get("restored", 0) + 1
        return json.dumps(orig, ensure_ascii=False)[1:-1] if escape else orig

    out = _PLACEHOLDER_RX.sub(_sub, confirmed)
    # 第二遍：捞回被模型剥了花括号/只剩一半的占位符（只认查得到的）
    if "_" in out:
        def _loose_sub(m):
            whole = m.group(0)
            if whole.startswith("{{") and whole.endswith("}}"):
                return whole
            orig = _lookup("{{" + m.group(1) + "}}", sid)
            if orig is None: return whole
            s["degraded"] = s.get("degraded", 0) + 1
            return json.dumps(orig, ensure_ascii=False)[1:-1] if escape else orig
        out = _LOOSE_PLACEHOLDER_RX.sub(_loose_sub, out)
    return out
```

**Go 移植要点**：`_PARTIAL_RX` 检测尾部未闭合占位符 → 从末尾找最后一个 `{{`，其后无 `}}` 则暂存；`json.dumps(...)[1:-1]` 转义 → Go 用 `strconv.Quote` 去首尾引号。

---

## 五、流式 SSE 还原（最难模块）

### 5.1 增量文本槽位识别
> 源码：`transparent.py:3030-3096`

```python
def _sse_text_slots(data):
    """列出 SSE 事件里的增量文本槽位：[(channel, text, setter, escape)]。
    每个字段必须用独立通道——共用缓冲会把上一个字段的尾巴吐进下一个字段。"""
    slots = []
    if not isinstance(data, dict):
        return slots
    # OpenAI Chat Completions 流
    for idx, c in enumerate(data.get("choices", []) or []):
        d = c.get("delta")
        if isinstance(d, dict):
            if isinstance(d.get("content"), str):
                slots.append((f"c{idx}.content", d["content"], _setter(d, "content"), False))
            if isinstance(d.get("reasoning_content"), str):
                slots.append((f"c{idx}.reason", d["reasoning_content"], _setter(d, "reasoning_content"), False))
            for tidx, tc in enumerate(d.get("tool_calls", []) or []):
                fn = tc.get("function") if isinstance(tc, dict) else None
                # 槽位键用 tool_calls[].index（协议标明属于第几个工具），不能用数组下标
                slot_no = tc.get("index") if isinstance(tc, dict) and isinstance(tc.get("index"), int) else tidx
                if isinstance(fn, dict) and isinstance(fn.get("arguments"), str):
                    slots.append((f"c{idx}.tool{slot_no}", fn["arguments"], _setter(fn, "arguments"), True))  # escape=True
    # Anthropic Messages 流
    etype = data.get("type") if isinstance(data.get("type"), str) else ""
    if etype == "content_block_delta":
        d = data.get("delta")
        blk = data.get("index", 0)
        if isinstance(d, dict):
            if isinstance(d.get("text"), str):
                slots.append((f"a{blk}.text", d["text"], _setter(d, "text"), False))
            if isinstance(d.get("partial_json"), str):
                slots.append((f"a{blk}.pj", d["partial_json"], _setter(d, "partial_json"), True))
    # OpenAI Responses API 流
    if etype == "response.output_text.delta" and isinstance(data.get("delta"), str):
        slots.append(("r%s.text" % data.get("output_index", 0), data["delta"], _setter(data, "delta"), False))
    elif etype == "response.function_call_arguments.delta" and isinstance(data.get("delta"), str):
        slots.append(("r%s.args" % data.get("output_index", 0), data["delta"], _setter(data, "delta"), True))
    return slots
```

### 5.2 单事件还原
> 源码：`transparent.py:3191-3228`

```python
def _restore_sse_event(block, sid, final=False):
    """还原一个 SSE 事件块。按事件粒度处理是流式透传的前提。"""
    out_lines = []
    for line in block.split("\n"):
        stripped = line.rstrip("\r")
        if stripped.startswith("data:"):
            payload = stripped[5:].lstrip(" ")
            if payload.strip() == "[DONE]":
                tail = _flush_pending(sid)  # [DONE] 之前必须把缓冲吐净
                if tail:
                    out_lines.append("")
                    out_lines.extend(tail.rstrip("\n").split("\n"))
                    out_lines.append("")
                out_lines.append(line)
                continue
            try:
                data = json.loads(payload)
            except json.JSONDecodeError:
                out_lines.append("data: " + restore(payload, sid, channel="raw", final=final))
                continue
            if _sse_is_terminal(data):
                tail = _flush_pending(sid)  # 终止事件前吐净
                if tail:
                    out_lines.append("")
                    out_lines.extend(tail.rstrip("\n").split("\n"))
                    out_lines.append("")
            _restore_sse_data(data, sid, final=final)
            out_lines.append("data: " + json.dumps(data, ensure_ascii=False))
            continue
        out_lines.append(line)
    return "\n".join(out_lines)
```

### 5.3 流回调核心（按事件边界切分 + 中途不返回空）
> 源码：`transparent.py:3280-3338`

```python
    def _stream(data: bytes):
        if state["done"]:
            return data
        state["calls"] += 1
        state["bytes_in"] += len(data)
        last = not data
        state["buf"] += state["decoder"].decode(data, final=last)
        state["buf"] = state["buf"].replace("\r\n", "\n")  # CRLF 兼容
        out = []
        # SSE 事件以空行分隔；只处理已完整到达的事件，半个事件留在缓冲里
        while True:
            idx = state["buf"].find("\n\n")
            if idx < 0:
                break
            block, state["buf"] = state["buf"][:idx], state["buf"][idx + 2:]
            out.append(_restore_sse_event(block, sid, final=False) + "\n\n")
        if last:
            if state["buf"]:
                out.append(_restore_sse_event(state["buf"], sid, final=True))
                state["buf"] = ""
            tail = _flush_pending(sid)  # 收尾：把残留补发，避免吞掉最后几个字
            if tail:
                out.append(tail)
        text = "".join(out)
        if last:
            _finish()
            return text.encode("utf-8")
        # 关键：中途块绝不能返回 b""——会被按 chunked 语法写成终止块，
        # 客户端据此判定响应结束、停止读取并关连接
        return text.encode("utf-8") if text else b""  # NovaVeil: 返回空列表不写出
```

**Go 移植要点**：NovaVeil 已有 SSE 逐 event 解码路径（`relay/protocol.go` + transformer），还原应在解码出的文本增量上操作，而非直接对编码字节做字符串替换。`_sse_text_slots` 的槽位识别逻辑须按三种协议分别适配。

---

## 六、校验函数（防误报）

> 源码：`transparent.py:410-612`

```python
def _luhn_ok(num: str) -> bool:
    """Luhn 校验（银行卡）。"""
    digits = [int(c) for c in num if c.isdigit()]
    if len(digits) < 12:
        return False
    s, dbl = 0, False
    for d in reversed(digits):
        if dbl:
            d = d * 2 - 9 if d > 4 else d * 2
        s += d
        dbl = not dbl
    return s % 10 == 0

_IDCARD_W = (7, 9, 10, 5, 8, 4, 2, 1, 6, 3, 7, 9, 10, 5, 8, 4, 2)
_IDCARD_CODE = "10X98765432"
_PROVINCES = {"11","12","13","14","15","21","22","23","31","32","33","34","35",
              "36","37","41","42","43","44","45","46","50","51","52","53","54",
              "61","62","63","64","65","71","81","82"}

def _idcard18_ok(num: str) -> bool:
    """18 位身份证：省份 + 真实日期 + ISO 7064 校验位。"""
    if not isinstance(num, str) or len(num) != 18 or not num[:17].isdigit():
        return False
    if num[:2] not in _PROVINCES:
        return False
    try:
        y, m, d = int(num[6:10]), int(num[10:12]), int(num[12:14])
        birth = datetime.date(y, m, d)
        if not (1880 <= birth.year <= datetime.datetime.now().year):
            return False
    except ValueError:
        return False
    total = sum(int(d) * w for d, w in zip(num[:17], _IDCARD_W))
    return num[17].upper() == _IDCARD_CODE[total % 11]

def _phone_ok(num_str: str) -> bool:
    """国内手机号：1[3-9] 开头 + 排除全同号。"""
    digits = re.sub(r"\D", "", str(num_str or ""))
    if digits.startswith("86") and len(digits) == 13:
        digits = digits[2:]
    if len(digits) != 11:
        return False
    if not (digits[0] == "1" and digits[1] in "3456789"):
        return False
    if len(set(digits)) == 1:  # 排除 11111111111
        return False
    return True

def _email_ok(email_str: str) -> bool:
    """邮箱：合法用户名/域名 + TLD≥2 + 无连续双点。"""
    s = str(email_str or "").strip()
    if "@" not in s or s.startswith("@") or s.endswith("@"):
        return False
    local, domain = s.split("@")[0], s.split("@")[1]
    if len(local) < 1 or len(domain) < 3 or "." not in domain:
        return False
    if local.startswith(".") or ".." in local or ".." in domain:
        return False
    if len(domain.split(".")[-1]) < 2:
        return False
    return True

def _jwt_ok(token: str) -> bool:
    """JWT：header base64 解码后必须含 alg。只按三段形态匹配会误判长 base64 串。"""
    try:
        head = token.split(".")[0]
        pad = "=" * (-len(head) % 4)
        decoded = base64.urlsafe_b64decode(head + pad).decode("utf-8", errors="replace")
        return '"alg"' in decoded
    except Exception:
        return False

def _iban_ok(iban: str) -> bool:
    """IBAN mod-97：字母 A=10..Z=35，前 4 位移尾后整体 mod 97 余 1。"""
    s = iban.strip()
    if len(s) < 15 or not re.fullmatch(r"[A-Z]{2}\d{2}[A-Z0-9]{11,30}", s):
        return False
    reordered = s[4:] + s[:4]
    digits = "".join(str(ord(c) - 55) if c.isalpha() else c for c in reordered)
    return int(digits) % 97 == 1
```

**Go 移植要点**：全部是纯函数，直接逐行翻译。`datetime.date` → `time.Date`；`base64.urlsafe_b64decode` → `base64.RawURLEncoding.DecodeString`。

---

## 七、凭据安全

> 源码：`transparent.py:864-927`

```python
# 凭据的结构前缀——公开格式标记，原样显示零风险
_CRED_PREFIX_RX = re.compile(
    r"^(?:sk-proj-|sk-ant-[a-z0-9]{2,10}-|sk-|gh[pousr]_|github_pat_|AIza|AKIA|ASIA"
    r"|AKID|LTAI|xox[baprs]-|[sr]k_(?:live|test)_|cli_|ding|eyJ)"
)
# 高熵凭据：显示「前缀 + 末 4 位」足够定位，剩余熵仍天文数字级
_HIGH_ENTROPY_LABELS = {"API_KEY", "ACCESS_KEY", "TOKEN", "JWT"}

def _cred_preview(orig, label):
    """凭据预览：高熵类显示前缀+末4位，低熵口令不露首尾。"""
    if label in _HIGH_ENTROPY_LABELS:
        m = _CRED_PREFIX_RX.match(orig)
        prefix = m.group() if m else orig[:4]
        return prefix + "…" + orig[-4:] if len(orig) > len(prefix) + 4 else prefix + "…"
    return "…"  # 低熵口令（SECRET/CONNSTR）完全不露

def _cred_digest(orig):
    """凭据摘要：sha256 前 16 位 hex。"""
    return hashlib.sha256(orig.encode("utf-8")).hexdigest()[:16]
```

**Go 移植要点**：`hashlib.sha256` → `crypto/sha256`；NovaVeil `op/error_log.go` 已有 redact 雏形，可强化为同策略。

---

## 八、内置正则规则（节选最高危的 5 条）

> 源码：`transparent.py:52-114`

```python
RULES = [
    # PEM 私钥整块替换（最高危，形态固定零误报）
    (re.compile(r"-----BEGIN (?:RSA |EC |DSA |OPENSSH |PGP )?PRIVATE KEY-----[\s\S]{20,}?-----END[^-]*PRIVATE KEY-----"), "PRIVATE_KEY", 0),
    # GitHub PAT
    (re.compile(r"(?<![A-Za-z0-9_-])(?:ghp|gho|ghu|ghs|ghr)_[A-Za-z0-9_]{20,}(?![A-Za-z0-9_-])"), "API_KEY", 0),
    # GitHub fine-grained PAT
    (re.compile(r"(?<![A-Za-z0-9_-])github_pat_[A-Za-z0-9_]{50,}(?![A-Za-z0-9_-])"), "API_KEY", 0),
    # 连接串密码：只脱密码组（第2组），保留 scheme/user/host
    (re.compile(r"(?i)\b[a-z][a-z0-9+.-]*://[^\s:@/]+:([^\s@/]{4,})@"), "CONNSTR", 1),
    # SECRET 赋值凭据：中英文关键词 + 半角全角分隔符 + 值须含数字/符号
    (re.compile(
        r"(?i)(?:(?<![A-Za-z0-9_.])(?:password|passwd|pwd|secret|token|api[_-]?key"
        r"|access[_-]?key|private[_-]?key)(?![A-Za-z0-9_.])"
        r"|(?:密码|口令|令牌|密钥|秘钥|密匙|凭据|凭证|私钥|授权码|访问密钥|接口密钥))"
        r"[\"'“”「」]?\s*[:=：＝]\s*[\"'“”「」]?(?!/)"
        r"(?=[A-Za-z0-9!@#$%^&*_~+=-]*[0-9!@#$%^&*])"
        r"([A-Za-z0-9!@#$%^&*_~+=-]{6,64})(?![A-Za-z0-9!@#$%^&*_~+=-])"
    ), "SECRET", 1),
    # ... 其余 14 条
]
```

**Go 移植要点**：正则语法基本通用，`[\s\S]` → Go 用 `(?s)`；命名捕获组 Go 不支持，用编号组。完整 19 条见 maskit `transparent.py:52-230`。
